<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hexin.springboot.dubbo.consumer.mapper.LikeCountMapper">
    <sql id="Base_Column_list">
        id
        ,liked_member_id,liked_type_id,liked_type,like_total,del_flag,create_by,create_time,update_by,update_time
    </sql>

    <!--<select id="selectByParamForUpdate" resultType="com.youtai.saas.member.api.entity.LikeCount">

        select  <include refid="Base_Column_list"></include> from t_like_count
        <where>
            del_flag='0'
            <if test="likedMemberId != null and likedMemberId != ''">
                and liked_member_id = #{likedMemberId}
            </if>
            <if test="likedTypeId != null and likedTypeId != ''">
                and liked_type_id = #{likedTypeId}
            </if>
            <if test="likedType != null and likedType != ''">
                and liked_type = #{likedType}
            </if>
        </where>
        for update

    </select>-->

</mapper>
